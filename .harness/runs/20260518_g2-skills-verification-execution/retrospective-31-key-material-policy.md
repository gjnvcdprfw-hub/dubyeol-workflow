# 회고 31 후보 — 외부 도구 호출 폴백 시 키 자료 이동 처리

**run ID**: 20260518_g2-skills-verification-execution  
**작성일시**: 2026-05-17 KST  
**작성자**: [Foreman]

---

## 1. 관찰 사실

G-2 SUB-3 진행 중 Codex [Reviewer] 호출이 지역·네트워크 문제로 실패했고, [Owner] 승인에 따라 지피티 [Reviewer] 폴백으로 전환했다. 이 과정에서 `silkroadhub`의 `scripts/load_openai_key.sh`를 `dubyeol-workflow` 마스터의 `scripts/load_openai_key.sh`로 복사해 로컬 지피티 호출에 사용했다.

이 처리는 단기적으로 GPT 호출을 가능하게 했지만, 마스터 저장소와 클라이언트 저장소의 분리 원칙 관점에서는 부정합이 있다.

---

## 2. 부정합

| 항목 | 내용 | 리스크 |
|---|---|---|
| 마스터의 클라이언트 키 자료 의존 | `dubyeol-workflow`가 `silkroadhub`의 키 로드 파일을 복사해 사용 | 마스터/클라이언트 분리 의미 약화 |
| 키 파일 잔존 | 복사된 키 파일이 마스터에 남을 경우 git status·commit 과정에서 노출 가능 | GitHub 노출 위험 |
| 폴백 절차 불명확 | 외부 도구 호출 폴백 시 키를 어디서 어떻게 로드할지 정책 부재 | task마다 임시 복사 재발 가능 |

---

## 3. 정책 task 후보

| 정책 항목 | 제안 기준 |
|---|---|
| 마스터/클라이언트별 키 보유 | 각 저장소는 자기 실행에 필요한 `.env` 또는 시스템 환경변수를 별도로 보유한다. |
| 키 로드 방식 | 각자 시스템 환경변수 또는 저장소별 `.env`를 사용하고, 다른 저장소의 키 파일을 복사하지 않는다. |
| 복사 금지 | 클라이언트 키 자료를 마스터로 복사하지 않는다. 반대 방향도 동일하게 금지한다. |
| 즉시 정리 | 예외적으로 복사된 키 자료가 생기면 즉시 삭제하고 git status로 잔존 여부를 검증한다. |
| `.gitignore` | `.env`, 키 로드 스크립트, 외부 API secret 파일 패턴을 ignore 대상으로 명시한다. |
| 종료 검증 | final-report 전 `git status --short`, 키 파일 후보 탐색, `.gitignore` 적용 여부를 확인한다. |

---

## 4. 본 run에서의 즉시 조치 계획

1. `dubyeol-workflow/scripts/load_openai_key.sh` 잔존 여부와 git 추적 여부를 확인한다.
2. 복사된 키 파일을 즉시 삭제한다.
3. `.gitignore`에 키·환경변수 파일 패턴이 없으면 보강한다.
4. final-report에는 `silkroadhub`는 키 로드 파일 출처로만 사용되었고, 복사본은 종료 전 정리했다는 사실을 명시한다.

---

**회고 31 후보 기록 끝.**
