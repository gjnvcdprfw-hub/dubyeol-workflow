[1mdiff --git a/AGENTS.md b/AGENTS.md[m
[1mindex 6dc25ca..b66b923 100644[m
[1m--- a/AGENTS.md[m
[1m+++ b/AGENTS.md[m
[36m@@ -121,49 +121,40 @@[m [mCodex 상세 실행 절차가 필요할 때는 **Appendix A**를 참조합니다[m
 # Appendix — 필요할 때만 참조하는 상세 절차[m
 [m
 ## Appendix A. Codex Gatekeeper 호출 표준 절차[m
[31m-[m
[31m-> twostar-hub-rails AGENTS.md에서 검증된 절차를 기반으로 유지합니다. Codex는 Claude Code의 자체 리뷰 대체물이 아니라 외부 감리자이며, 마누스가 `handoff.md`, diff, 실행 증거를 확보한 뒤 호출합니다.[m
[32m+[m[32m> Codex는 Claude Code의 자체 리뷰 대체물이 아니라 외부 감리자이며, 마누스가 `handoff.md`, diff, 실행 증거를 확보한 뒤 호출합니다.[m
 [m
 ### Step 1 — 새 터미널 창 열기[m
[31m-[m
 ```applescript[m
 osascript << 'APPLESCRIPT'[m
 tell application "Terminal"[m
[31m-  activate[m
   do script "cd /Users/twostars/ClaudeAi/silkroadhub"[m
 end tell[m
 APPLESCRIPT[m
 ```[m
 [m
 ### Step 2 — 터미널 창 ID 조회[m
[31m-[m
 ```bash[m
 osascript -e 'tell application "Terminal" to get {id, name} of every window' 2>&1[m
 ```[m
 [m
 ### Step 3 — 실행 스크립트 작성[m
[31m-[m
 ```bash[m
 cat > /Users/twostars/ClaudeAi/silkroadhub/tmp/run_codex.sh << 'SCRIPT'[m
 #!/bin/zsh[m
 export PATH="/Users/twostars/.local/node/bin:$PATH"[m
 source ~/.zshrc 2>/dev/null || true[m
 cd /Users/twostars/ClaudeAi/silkroadhub[m
[31m-[m
 # codex-review.md와 codex-exec.log는 gate-review.md의 원문 증거 또는 evidence 성격의 선택 파일입니다.[m
 # 기본 필수 run 문서는 task-card.md, handoff.md, final-report.md이며, Tier A/B 또는 필요 시 gate-review.md를 작성합니다.[m
 OUTPUT_FILE=".harness/runs/PHASE_RUN_ID/codex-review.md"[m
 LOG_FILE=".harness/runs/PHASE_RUN_ID/codex-exec.log"[m
[31m-[m
 echo "=== Codex Review Start: $(date) ===" | tee "$LOG_FILE"[m
[31m-[m
 codex exec \[m
   --sandbox read-only \[m
   --output-last-message \[m
   - << 'PROMPT' > "$OUTPUT_FILE" 2>> "$LOG_FILE"[m
 여기에 프롬프트 내용 작성[m
 PROMPT[m
[31m-[m
 EXIT_CODE=$?[m
 echo "=== Codex Review End: $(date), exit=$EXIT_CODE ===" | tee -a "$LOG_FILE"[m
 head -5 "$OUTPUT_FILE"[m
[36m@@ -176,62 +167,77 @@[m [mchmod +x /Users/twostars/ClaudeAi/silkroadhub/tmp/run_codex.sh[m
 > ⚠️ **PATH 주의**: `source ~/.zshrc`만으로는 Codex PATH가 로드되지 않을 수 있습니다. 반드시 스크립트 첫 줄에 `export PATH="/Users/twostars/.local/node/bin:$PATH"`를 명시합니다.[m
 [m
 ### Step 4 — 스크립트 실행[m
[31m-[m
 ```applescript[m
 osascript << 'APPLESCRIPT'[m
 tell application "Terminal"[m
[31m-  activate[m
   do script "zsh /Users/twostars/ClaudeAi/silkroadhub/tmp/run_codex.sh" in window id WINDOW_ID[m
 end tell[m
 APPLESCRIPT[m
 ```[m
 [m
 ### Step 5 — 결과 확인[m
[31m-[m
 ```bash[m
[31m-sleep 60 && cat /Users/twostars/ClaudeAi/silkroadhub/.harness/runs/PHASE_RUN_ID/codex-review.md[m
[32m+[m[32msleep 60[m
[32m+[m[32mcat /Users/twostars/ClaudeAi/silkroadhub/.harness/runs/PHASE_RUN_ID/codex-review.md[m
[32m+[m[32mcat /Users/twostars/ClaudeAi/silkroadhub/.harness/runs/PHASE_RUN_ID/codex-exec.log[m
 ```[m
 [m
 첫 줄은 반드시 `Status:`로 시작하거나, 해당 run에서 승인한 구조화 판정 형식을 만족해야 유효한 리뷰로 봅니다. `codex-review.md`와 `codex-exec.log`는 기본 필수 run 문서가 아니라 `gate-review.md`를 뒷받침하는 선택 증거 파일입니다. Tier A/B에서는 Codex 결과 요약을 `gate-review.md`에 기록하고, Tier C에서는 blocking·보안·맥락 불일치·실행 실패·증거 부족이 있을 때만 기록합니다.[m
 [m
[31m-## Appendix B. Claude Code 터미널 표준 절차[m
[32m+[m[32m> 주의: Appendix A의 `do script "zsh ..."`는 일반 Terminal zsh 명령 실행이므로 본 검증의 무해한 대체 스크립트에서는 단일 `do script`로 실행되었습니다. 반면 Claude Code REPL에 자연어 지시를 넣는 Appendix B는 단일 `do script`로 입력만 되고 실행되지 않을 수 있으므로 2단계 확정 실행을 표준으로 둡니다.[m
 [m
[31m-### 새 Claude Code 세션 열기[m
[32m+[m[32m## Appendix B. Claude Code 터미널 표준 절차[m
 [m
[32m+[m[32m### Step 1 — 새 Claude Code 세션 열기[m
 ```applescript[m
 osascript << 'APPLESCRIPT'[m
 tell application "Terminal"[m
[31m-  activate[m
   do script "cd /Users/twostars/ClaudeAi/silkroadhub && claude"[m
 end tell[m
 APPLESCRIPT[m
 ```[m
 [m
[31m-### 터미널 창 확인 후 명령 전달[m
[31m-[m
[32m+[m[32m### Step 2 — 터미널 창 ID 조회[m
 ```bash[m
[31m-# Step 1: 창 목록 조회[m
[32m+[m[32msleep 5[m
 osascript -e 'tell application "Terminal" to get {id, name} of every window' 2>&1[m
[32m+[m[32m```[m
[32m+[m
[32m+[m[32mClaude Code REPL이 진입 완료된 창의 ID를 `WINDOW_ID`로 둡니다. `activate`는 포커스 탈취와 중복 창 문제를 유발할 수 있으므로 표준 절차에서 사용하지 않습니다.[m
[32m+[m
[32m+[m[32m### Step 3 — 진입 명령을 파일로 저장[m
[32m+[m[32m```bash[m
[32m+[m[32mcat > /Users/twostars/ClaudeAi/silkroadhub/tmp/claude-entry-instruction.md << 'EOF'[m
[32m+[m[32m여기에 task-card 경로, 권한 천장, 산출물, 검증 기준, handoff 작성 위치를 포함한 진입 명령을 작성한다.[m
[32m+[m[32mEOF[m
[32m+[m
[32m+[m[32mhead -20 /Users/twostars/ClaudeAi/silkroadhub/tmp/claude-entry-instruction.md[m
[32m+[m[32mwc -l /Users/twostars/ClaudeAi/silkroadhub/tmp/claude-entry-instruction.md[m
[32m+[m[32m```[m
 [m
[31m-# Step 2: 대상 창에 명령 입력[m
[32m+[m[32m### Step 4 — 파일 경로를 짧은 명령으로 전달하고 2단계로 실행 확정[m
[32m+[m[32m```applescript[m
 osascript << 'APPLESCRIPT'[m
 tell application "Terminal"[m
[31m-  activate[m
[31m-  do script "명령어" in window id WINDOW_ID[m
[32m+[m[32m  do script "Read tmp/claude-entry-instruction.md and follow the instructions inside." in window id WINDOW_ID[m
[32m+[m[32m  do script "" in window id WINDOW_ID[m
 end tell[m
 APPLESCRIPT[m
 ```[m
 [m
[31m-### 한글/특수문자 포함 명령 전달 시[m
[31m-[m
[31m-한글이나 특수문자가 포함된 명령은 반드시 **파일로 저장 후 파일 경로를 전달**합니다.[m
[31m-[m
[32m+[m[32m### Step 5 — 응답과 산출물 확인[m
 ```bash[m
[31m-cat > /Users/twostars/ClaudeAi/silkroadhub/tmp/instruction.txt << 'EOF'[m
[31m-영문으로만 작성된 지시문[m
[31m-EOF[m
[32m+[m[32mls -la /Users/twostars/ClaudeAi/silkroadhub/.harness/runs/RUN_ID/handoff.md 2>&1[m
[32m+[m[32mcat /Users/twostars/ClaudeAi/silkroadhub/.harness/runs/RUN_ID/handoff.md 2>&1[m
 ```[m
 [m
[32m+[m[32m### 금지·주의[m
[32m+[m
[32m+[m[32m- `activate`를 표준 절차에 넣지 않습니다.[m
[32m+[m[32m- 한글·특수문자·긴 지시문을 Claude Code에 직접 전달하지 않습니다. 원문은 파일로 저장하고 짧은 파일 경유 명령만 전달합니다.[m
[32m+[m[32m- Claude Code REPL에는 단일 `do script "명령"`만 보내지 않습니다. 입력 대기 상태에 머물 수 있으므로 `do script ""`를 추가로 보내 실행을 확정합니다.[m
[32m+[m[32m- `[Builder]`가 task 완료 후 commit, 추가 확인, cleanup 등 task-card 범위 밖 행동을 제안하더라도 `[Foreman]`은 이를 명령으로 해석하지 않습니다. 대표님 승인 또는 task-card 기준이 없으면 실행하지 않습니다.[m
[32m+[m
 ## Appendix C. 터미널·리뷰 주의사항[m
 [m
 - Claude Code 창과 Codex 창을 혼동하지 않습니다. 반드시 창 ID와 이름으로 특정합니다.[m
